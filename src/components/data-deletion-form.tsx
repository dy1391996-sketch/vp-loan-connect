"use client";

import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Field, Input, Textarea } from "@/components/ui/form";

export function DataDeletionForm() { const [name,setName]=useState(""); const [mobile,setMobile]=useState(""); const [requestId,setRequestId]=useState(""); const [code,setCode]=useState(""); const [token,setToken]=useState(""); const [reason,setReason]=useState(""); const [status,setStatus]=useState(""); const [busy,setBusy]=useState(false); async function otp() { setBusy(true); const r=await fetch("/api/otp/request",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({fullName:name,mobile,source:"data_deletion"})}); const d=await r.json(); if(r.ok){setRequestId(d.requestId);setStatus(d.developmentCode?`Development OTP: ${d.developmentCode}`:"OTP sent.");}else setStatus(d.error);setBusy(false); } async function verify(){setBusy(true);const r=await fetch("/api/otp/verify",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({requestId,mobile,code})});const d=await r.json();if(r.ok){setToken(d.verificationToken);setStatus("Mobile verified.");}else setStatus(d.error);setBusy(false);} async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const r=await fetch("/api/data-deletion",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({verificationToken:token,mobile,reason})});const d=await r.json();setStatus(r.ok?`Request recorded. Reference: ${d.requestId}`:d.error);setBusy(false);} return <form onSubmit={submit} className="mt-8 rounded-2xl border border-line bg-surface p-5 sm:p-7"><div className="grid gap-5 sm:grid-cols-2"><Field label="Full name"><Input value={name} onChange={(e)=>setName(e.target.value)} required /></Field><Field label="Verified mobile"><div className="flex gap-2"><Input value={mobile} onChange={(e)=>setMobile(e.target.value.replace(/\D/g,"").slice(0,10))} maxLength={10} required disabled={Boolean(token)} /><Button type="button" variant="secondary" onClick={otp} disabled={busy||Boolean(token)}>Send OTP</Button></div></Field>{requestId&&!token?<Field label="OTP"><div className="flex gap-2"><Input value={code} onChange={(e)=>setCode(e.target.value.replace(/\D/g,"").slice(0,6))} /><Button type="button" onClick={verify} disabled={busy}>Verify</Button></div></Field>:null}<div className="sm:col-span-2"><Field label="Reason (optional)"><Textarea value={reason} onChange={(e)=>setReason(e.target.value)} maxLength={1000} /></Field></div></div>{status?<p className="mt-4 text-sm leading-6 text-slate-600">{status}</p>:null}<Button type="submit" className="mt-5" disabled={busy||!token}>Submit deletion request</Button></form>; }
