import Link from "next/link";

export type PolicySection = { title: string; paragraphs?: string[]; bullets?: string[] };
export function PolicyPage({ title, summary, sections, updated = "23 July 2026" }: { title: string; summary: string; sections: PolicySection[]; updated?: string }) { return <section className="bg-surface py-12 sm:py-16"><div className="page-shell"><article className="mx-auto max-w-4xl rounded-3xl border border-line bg-white p-6 shadow-card sm:p-10"><p className="text-xs font-bold uppercase tracking-[0.18em] text-brand-700">Legal & policy</p><h1 className="mt-3 text-3xl font-bold tracking-[-0.04em] text-navy-950 sm:text-4xl">{title}</h1><p className="mt-5 text-base leading-8 text-slate-600">{summary}</p><p className="mt-4 text-xs text-slate-500">Last updated: {updated} • Owner legal review required before production launch.</p><div className="mt-10 grid gap-9">{sections.map((section) => <section key={section.title}><h2 className="text-xl font-bold text-navy-950">{section.title}</h2>{section.paragraphs?.map((p) => <p key={p} className="mt-3 text-sm leading-7 text-slate-600">{p}</p>)}{section.bullets ? <ul className="mt-3 grid gap-2 pl-5 text-sm leading-7 text-slate-600">{section.bullets.map((item) => <li key={item} className="list-disc">{item}</li>)}</ul> : null}</section>)}</div><div className="mt-10 border-t border-line pt-6 text-sm text-slate-600">Questions? <Link href="/contact" className="font-bold text-brand-700">Contact VP Loan Connect support</Link>.</div></article></div></section>; }
