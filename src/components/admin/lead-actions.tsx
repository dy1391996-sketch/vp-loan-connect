"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { LEAD_STAGE_LABELS } from "@/lib/constants";
export function LeadActions({id,initialStage,initialNotes}:{id:string;initialStage:string;initialNotes:string}){const[stage,setStage]=useState(initialStage);const[notes,setNotes]=useState(initialNotes);const[contacted,setContacted]=useState(false);const[status,setStatus]=useState("");async function save(){setStatus("Saving…");const r=await fetch(`/api/admin/leads/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({stage,notes,markContacted:contacted})});setStatus(r.ok?"CRM update saved and audit logged.":"Unable to save update.");}return <div className="mt-5 rounded-2xl border border-line bg-white p-6"><h2 className="font-bold text-navy-950">CRM update</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Lead stage<select className="min-h-12 rounded-xl border border-line px-3" value={stage} onChange={e=>setStage(e.target.value)}>{Object.entries(LEAD_STAGE_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label className="grid gap-2 text-sm font-semibold">Internal notes<textarea className="min-h-28 rounded-xl border border-line p-3 font-normal" value={notes} onChange={e=>setNotes(e.target.value)} maxLength={5000}/></label></div><label className="mt-4 flex items-center gap-2 text-sm text-slate-600"><input type="checkbox" checked={contacted} onChange={e=>setContacted(e.target.checked)}/>Set “last contacted” to now</label><div className="mt-4 flex items-center gap-3"><Button type="button" onClick={save}>Save CRM update</Button><span className="text-xs text-slate-500">{status}</span></div></div>}
