"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Field,Input } from "@/components/ui/form";
type Values={reward:number;validationDays:number;minimumPayout:number;bonus5:number;bonus10:number;bonus25:number};
export function SettingsForm({initial}:{initial:Values}){const[v,setV]=useState(initial);const[status,setStatus]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setStatus("Saving…");const r=await fetch("/api/admin/settings/referrals",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(v)});setStatus(r.ok?"Settings saved and audit logged.":"Unable to save settings.");}return <form onSubmit={submit} className="rounded-2xl border border-line bg-white p-6"><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3"><Num label="Reward per ₹99 purchase" value={v.reward} set={n=>setV({...v,reward:n})}/><Num label="Validation days" value={v.validationDays} set={n=>setV({...v,validationDays:n})}/><Num label="Minimum payout" value={v.minimumPayout} set={n=>setV({...v,minimumPayout:n})}/><Num label="5-referral bonus" value={v.bonus5} set={n=>setV({...v,bonus5:n})}/><Num label="10-referral bonus" value={v.bonus10} set={n=>setV({...v,bonus10:n})}/><Num label="25-referral bonus" value={v.bonus25} set={n=>setV({...v,bonus25:n})}/></div><div className="mt-6 flex items-center gap-4"><Button type="submit">Save referral settings</Button><span className="text-sm text-slate-600">{status}</span></div></form>}
function Num({label,value,set}:{label:string;value:number;set:(n:number)=>void}){return <Field label={label}><Input type="number" min={0} value={value} onChange={e=>set(Number(e.target.value))}/></Field>}
