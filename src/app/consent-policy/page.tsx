import type { Metadata } from "next";
import { PolicyPage } from "@/components/legal/policy-page";
import { MARKETING_CONSENT_TEXT, SERVICE_CONSENT_TEXT } from "@/lib/constants";
export const metadata: Metadata = { title: "Consent and Communication Policy" };
export default function ConsentPolicyPage() { return <PolicyPage title="Consent and Communication Policy" summary="Service and marketing choices are separate, unselected by default and recorded with wording, version, time and source." sections={[{ title: "Mandatory service consent", paragraphs: [SERVICE_CONSENT_TEXT, "This is required only to generate and communicate about the requested assessment/report."] }, { title: "Optional marketing consent", paragraphs: [MARKETING_CONSENT_TEXT, "Declining this option does not prevent the requested service."] }, { title: "STOP and withdrawal", paragraphs: ["Sending STOP through the official WhatsApp channel records a withdrawal time, updates the lead's opt-out status and suppresses future promotional messages. One confirmation may be sent. Necessary service or transactional messages may still be delivered for an active request."] }, { title: "Consent records", bullets: ["Consent type, exact text and version", "Accepted/declined state and date/time", "Withdrawal date/time", "Source, user agent and referral context", "IP only when appropriate and enabled after review"] }, { title: "Lender referrals", paragraphs: ["A lender-specific referral requires a separate informed request/consent. General service or marketing consent does not automatically authorize sharing with a lender."] }]} />; }
