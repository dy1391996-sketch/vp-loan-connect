import type { Metadata } from "next";
import { ConsultationForm } from "@/components/consultation-form";
import { verifyAccessToken } from "@/lib/security/tokens";

export const dynamic = "force-dynamic"; export const metadata: Metadata = { title: "Consultation Request", robots: { index: false, follow: false } };
export default async function ConsultationPage({ searchParams }: { searchParams: Promise<{ assessment?: string; token?: string }> }) { const q = await searchParams; if (!q.assessment || !q.token) return <Restricted />; try { const payload = await verifyAccessToken(q.token, "result_access"); if (payload.sub !== q.assessment) throw new Error("mismatch"); } catch { return <Restricted />; } return <section className="min-h-[75vh] bg-surface py-14"><div className="page-shell"><div className="mx-auto max-w-2xl"><p className="text-xs font-bold uppercase tracking-[0.18em] text-brand-700">15-minute WhatsApp consultation</p><h1 className="mt-3 text-3xl font-bold tracking-[-0.04em] text-navy-950">Consultation request</h1><p className="mt-3 mb-7 text-sm leading-7 text-slate-600">Guidance only. The consultation is not a lender call and does not guarantee approval.</p><ConsultationForm assessmentId={q.assessment} token={q.token} /></div></div></section>; }
function Restricted() { return <section className="min-h-[60vh] bg-surface py-20"><div className="page-shell text-center"><h1 className="text-2xl font-bold text-navy-950">Secure assessment link required</h1></div></section>; }
