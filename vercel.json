{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "env": {
    "PAYMENT_PROVIDER": "cashfree"
  },
  "buildCommand": "pnpm typecheck && pnpm lint && pnpm test && pnpm run db:deploy && pnpm exec prisma generate && pnpm run db:seed && pnpm build && pnpm verify:readiness"
}
